# port-lint Proposed Changes

**Generated:** 2026-08-28
**Source:** ratatui-core/src/buffer/buffer.rs
**Target:** src/commonMain/kotlin/ratatui/buffer/Buffer.kt

These are review proposals only. They are emitted when a Rust -> Kotlin pair matches only after fallback normalization, so the existing `port-lint` header is not an exact provenance match.

| Target file | Current header | Proposed header | Source path | Reason |
|-------------|----------------|-----------------|-------------|--------|
| `Buffer.kt` | `// port-lint: source ratatui-core/src/buffer/buffer.rs` | `// port-lint: source buffer.rs` | `buffer.rs` | `port-lint provenance header matched only by basename: 'ratatui-core/src/buffer/buffer.rs' vs expected 'buffer.rs'` |
